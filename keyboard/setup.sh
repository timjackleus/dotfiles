# Create keyboard layouts directory and copy layout
mkdir -p ~/Library/Keyboard\ Layouts/
cp $PWD/keyboard/tim.keylayout ~/Library/Keyboard\ Layouts/

echo 'Keyboard layout has been copied. Now add it in settings -> keyboard'
